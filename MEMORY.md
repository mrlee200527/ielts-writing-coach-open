# MEMORY.md

## 项目身份

- 项目名称：雅思作文助手
- 项目阶段：Working MVP 与 Functional UI v0.1 已通过自动验证、真实 MiMo Gate、Human Smoke 和 Human Visual 验收；保持在 `working-mvp-fast`，尚未合并 `main`
- 总指挥：用户

## 项目目标

构建 AI IELTS Writing Coach：学生在写作过程中获得适度、分阶段的实时帮助，而不是只在完成全文后接受一次性批改。第一版聚焦 IELTS Academic Writing Task 1，并为后续 Task 2 扩展保留边界。

## 已确认产品范围

- Task 1 题目包含图表或图片，系统需要理解并结构化题目信息。
- Task 1 MVP 覆盖全部四类题目：动态、静态、流程图和地图。
- 句子完成后进行轻量语言检查；段落完成后分析结构、逻辑、数据比较和任务完成度；全文完成后按 IELTS 四项标准评分总结。
- 系统必须控制干预时机和反馈数量，允许继续观察或不提示，避免学生依赖。
- 长期记录高频错误、已掌握表达、薄弱技能、目标分数和学习进步。
- 当前 Essay State 与跨会话 Student Memory 必须分离。
- IELTS 官方评分标准与教师教学方法必须分源管理，不得混为同一权威来源。
- 第一版以个人开发者可完成为约束，避免微服务和复杂知识库等过度设计。
- 核心用户为目标分数约 5.5–7.0、已了解基础写作结构的 IELTS Academic Task 1 自学学生。
- 默认产品体验采用“克制型、阶段门控式 Coach”：学生自由写作，系统在句子完成、段落完成和全文提交三个节点做不同深度判断，允许检查后不提示。
- 任一时刻最多展示 1 条句子反馈，每段最多展示 2 条主动反馈；同一问题不重复提醒，连续忽略两次同类提示后本篇抑制该类主动提示。
- 拼写可提供候选词但不自动替换；语法默认只指出位置与规则方向，主动展开后才给局部示例；结构反馈不得生成整段或固定模板。
- 题图识别不确定时，用户看到通俗风险说明和不确定类别，不显示内部置信度数字；不确定事实不得成为判定学生错误的唯一依据。
- 写作阶段不展示持续变化的预测分数或完整学习档案；提交后展示非官方总分估计、IELTS 四维分数、作文证据、唯一优先改进项和 2–3 个练习。
- Student Memory 面向用户称为“学习档案”；高频问题至少需要两篇已完成作文的证据，每条记录可查看依据并可删除，下一篇重点最多 1–2 项。
- 少于 150 词时提示风险但允许用户确认提交。
- 第一版明确不包含 Task 2、教师后台、教师知识库正式接入、整段或全文代写、模拟监考、语音或开放聊天、社区支付及复杂个性化课程。

## 协作角色

- 总指挥：汇总各智能体对话、下达指令并做最终决策。
- 架构 AI：负责系统架构、模块边界、技术选型、数据与接口契约、非功能需求、技术风险及实施拆分。
- 产品经理 AI：负责用户需求、产品范围、优先级、业务规则和验收标准。
- 程序员 AI：依据已确认需求和架构契约完成实现、单元测试与技术文档，并报告实现偏差。
- 测试 AI：负责测试策略、测试用例、缺陷与回归验证，并独立给出质量结论。
- 默认协作链路：产品经理 AI 定义“做什么” → 架构 AI 定义“如何组织实现” → 程序员 AI 实现 → 测试 AI 独立验证；重大变更由总指挥最终决定。

## 已确认决策

1. 项目使用根目录下的 `AGENTS.md`、`MEMORY.md`、`CHANGELOG.md` 维护多智能体协作上下文。
2. 用户输入独立指令 `init` 时，智能体必须按 `AGENTS.md` 规定的顺序完整读取上述三个文件。
3. `MEMORY.md` 只保存长期有效、已经确认的项目事实和决策。
4. `CHANGELOG.md` 保存带有智能体、时间和任务信息的追加式工作日志。
5. 四类 AI 的详细职责、输入输出、禁止越权事项和交接规则以 `AGENTS.md` 为准。
6. 第一版采用模块化单体边界设计；耗时 AI 工作流异步执行，编辑和自动保存不得依赖 AI 可用性。
7. 题图自动转换为结构化、带版本和置信度的 Task Context，不设置学生人工确认；低置信度通过自动重试、降级和置信度传播处理。
8. 实时反馈候选必须经过独立 Intervention Policy，输出立即显示、延迟、观察或抑制决策。
9. Student Memory 只能从已完成作文和可追溯证据中归纳；实时分析不得直接写入长期记忆。
10. LLM 调用经统一 Gateway，使用结构化输出、版本化 Prompt、模型分层、调用审计和失败降级。
11. 第一版系统架构基线记录于 `docs/ARCHITECTURE.md`；具体技术栈和供应商仍需总指挥确认。
12. 技术组合确认采用 Next.js；当前先本地运行，通过存储、文件、任务与 LLM 端口预留服务器能力，未来部署目标为 Supabase + Trigger.dev。
13. 默认 LLM 确认为 OpenAI `gpt-5.6-luna`，使用 Responses API、`reasoning.effort = low`、图像输入与结构化输出；业务模块只能通过 LLM Gateway 调用。
14. IELTS 官方评分标准 PDF 原件保存在 `docs/references/official/ielts/`，记录来源与 SHA-256，不直接修改；派生数据必须可追溯并经过复核。
15. 教师知识库本轮预留 `TeachingKnowledgePort`，在主写作链路完成后立即进入下一阶段，且继续与官方评分标准分源管理。
16. 项目采用 Git 管理代码、文档和轻量参考资料；本地配置、密钥、构建产物、运行数据和大体积原始音频必须忽略。
17. 任何实现步骤都必须在开始前定义可执行验证方法，并在完成后实际执行验证。验证失败不得进入下一步；未经验证的实现不得视为可用，也不得成为后续步骤依赖。程序员自测不能替代阶段结束后的 Test AI 独立验收；涉及真实用户使用的阶段还必须经过 Human Smoke Test。

## 当前约束

- 所有项目智能体均须遵守 `AGENTS.md`。
- 项目统一使用北京时间 `Asia/Shanghai`（UTC+08:00）记录日期和时间。
- 项目从第一版架构基线开始使用 Git 版本控制，默认分支为 `main`。
- 大体积教师原始音频保留在本地，不进入普通 Git；转录、来源元数据和知识条目纳入版本控制。
- 实施必须逐步设置可执行验证 gate；任一 gate 失败即停止后续依赖步骤。阶段结束仍须 Test AI 独立验收，真实用户链路还须 Human Smoke Test。

## 待决策事项

- LLM 预算、区域和数据保留约束。
